<form accept-charset="UTF-8" action="/admin/users/index">
  <input name="utf8" type="hidden" value="&#x2713;"/>
  <label for="q">Search for:</label>
  <input id="q" name="keywords" type="text" placeholder='seperate keywords by ","'/>
  <input name="commit" type="submit" value="Search"/>
</form>


</br>
</br>
</br>
</br>

<%= form_tag("/admin/users/role") do %>
  <table class="table">
  <thead>
    <tr>
      <th>Select</th>
      <th>User name</th>
      <th>Email</th>
      <th>Role</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <%= check_box_tag('user_' + user.id.to_s, user.user_name) %>
        </td>
        <td>
          <%= link_to user.user_name, user_path(user) %></td>
        <td><%= user.email %></td>
        <td><%= user.role %></td>
      </tr>
    <% end %>
  </tbody>
</table>

  <%= select_tag(:new_role, options_for_select([['admin', 'admin'], ['cimaster', 'cimaster'], ['ordinary', 'ordinary']])) %>

  <%= submit_tag('Change role') %>
<% end %>
