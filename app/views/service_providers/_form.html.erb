<%= form_with(model: service_provider, local: true) do |form| %>
    <% if service_provider.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(service_provider.errors.count, "error") %> prohibited this service_provider from being
            saved:</h2>

          <ul>
            <% service_provider.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= form.label :app_id %>
      <%= form.text_field :app_id, id: :service_provider_app_id %>
    </div>

    <div class="field">
      <%= form.label :description %>
      <%= form.text_field :description, id: :service_provider_description %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
